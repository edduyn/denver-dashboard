# Claude Handoff: GitHub Push Instructions for Dashboard

To ensure the `index.html` dashboard updates are reflected live on GitHub Pages, follow these specific steps:

## 1. Local Repository Path
All work is performed within the local git repository:
`/Users/edduynpita/.gemini/antigravity/scratch/ANTIGRAVITY/2026_Goals_tracker/`

## 2. Remote Destination
The repository is linked to:
`https://github.com/edduyn/denver-dashboard.git` (Origin: `main` branch)

## 3. Standard Push Command
After making edits to `index.html`, run the following sequence to commit and push changes:

```bash
# Navigate to root (if not already there)
cd /Users/edduynpita/.gemini/antigravity/scratch/ANTIGRAVITY/2026_Goals_tracker/

# Stage changes
git add index.html

# Commit with a descriptive message
git commit -m "Literal text update for MTD data"

# Push to origin main
git push --quiet
```

## 4. Forced Visibility/Emergency Fixes
If the dashboard is appearing blank or not updating as expected, use the force push flag to override the remote state:

```bash
git push --quiet --force
```

## 5. Summary of Sync Methods
- **Manual (Gemini/Claude)**: Use the Git commands above.
- **Automated (Legacy)**: There is a `Update_Dashboard.command` and `src/scripts/run_morning_update.sh` for bulk processing of daily reports, but manual dashboard surgery should use the Git method for precision.

---
*Generated by Antigravity for Claude Handoff - Feb 6, 2026*
